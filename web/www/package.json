{
  "name": "helix-web-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Host page for the helix wasm port (alpha)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test"
  },
  "dependencies": {
    "helix-web": "file:../pkg",
    "@xterm/xterm": "^5.5.0",
    "@xterm/addon-fit": "^0.10.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0",
    "vite": "^6.3.0"
  }
}
